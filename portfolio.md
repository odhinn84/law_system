# 국회 법안 모니터링 시스템 (LegisWatch)

> 대한민국 국회 법안을 수집·분석하고, 시민이 자신의 삶과 연관된 법안을 쉽게 탐색할 수 있는 자동화 파이프라인 + 웹 서비스

---

## 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 기간 | 2026년 6월 |
| 유형 | 개인 프로젝트 (end-to-end) |
| 역할 | 기획 / 데이터 파이프라인 / LLM 프롬프트 엔지니어링 / 웹 UI 전체 |

**동기**: 국회에서 매일 수십 건의 법안이 발의되지만, 일반 시민이 자신의 직업·생활과 어떤 법안이 관련있는지 파악하기 어렵다. 열린국회정보 API와 로컬 LLM을 활용해 법안 내용을 자동 분석하고, 누구나 쉽게 탐색할 수 있는 서비스를 만들었다.

---

## 기술 스택

| 레이어 | 기술 |
|---|---|
| 데이터 수집 | Python, 열린국회정보 OpenAPI |
| 저장소 | SQLite, GitHub (data branch - CSV hosting) |
| LLM 분석 | Ollama + Gemma 4 (로컬 추론) |
| 웹 서비스 | Streamlit, Plotly (choropleth map) |
| 자동화 | cron (수집 매시간 / 분석 매시간 30분 / 배포 매일 새벽 3시) |
| 분류 체계 | 통계청 한국표준직업분류(KSCO) 8차 개정 |

---

## 시스템 아키텍처

```
[열린국회정보 API]
       │ collect.py (매시간)
       ▼
[SQLite: bills + members]
       │ analyze.py (매시간 30분)
       ▼
[LLM 분석 결과: bill_analysis]
       │ export.py (매일 새벽 3시)
       ▼
[GitHub data branch: bills.csv / members.csv]
       │
       ▼
[Streamlit 웹앱: st.cache_data로 CSV 로드]
```

파이프라인 전 단계가 자동화되어 있어 수집→분석→배포가 사람 개입 없이 운영된다.

---

## 주요 기능

### 1. 자동 데이터 수집 파이프라인
- 열린국회정보 API에서 법안 본문·제안이유·처리상태 수집
- 의원 정보(소속 정당, 지역구, 상임위) 병행 수집
- 중복 방지·신규 법안만 증분 수집 (upsert 방식)
- API 필드 누락 처리: `POLY_NM` 상시 공백 → `members.csv` 조인으로 정당 자동 보완

### 2. LLM 기반 법안 분석
로컬 Gemma 4 모델(Ollama)로 각 법안을 구조화된 JSON으로 분석:

```json
{
  "bill_summary": "시민 친화적 요약 (200자)",
  "core_change": "기존 대비 핵심 변경사항",
  "impact_direction": "positive / negative / mixed / neutral",
  "affected_occupations": ["KSCO 직업 중분류 태그"],
  "affected_ages": ["연령대 태그"],
  "affected_social": ["사회적 집단 태그"],
  "benefited_groups": ["혜택받는 집단"],
  "harmed_groups": ["불이익받는 집단"],
  "keywords": ["키워드"]
}
```

**프롬프트 엔지니어링 이슈 해결**:
- 초기 버전: `benefited_groups`에 "혜택을 받는 임차인", "부담이 늘어나는 선거관리위원회" 등 설명구가 혼입
- 원인: 지시어가 모호하여 LLM이 집단명 + 영향 설명을 함께 출력
- 해결: 명사구 10자 이내 제한, 금지 패턴 명시("~하는", "~에 따른"), 올바른/잘못된 예시 대조 제시
- 결과: "임차인", "소상공인", "전기차 이용자" 수준의 깔끔한 집단명 출력

### 3. 인터랙티브 웹 서비스 (Streamlit)

**홈**: 최신 법안 카드 리스트, 처리상태·직업군·영향방향 필터

**법안 검색**: 키워드 전문 검색 + 다중 필터 (정당, 처리상태, 직업군, 연령대)

**나에게 관련된 법안**: KSCO 직업 분류 / 연령대 / 사회적 집단 / 지역 선택 → 관련 법안 필터링

**의원별 법안**: 전국 choropleth 지도 → 시도 클릭/선택 → 해당 시도 의원 목록 + 시군구 필터 → 개별 의원 발의 법안 조회

**통계**: 처리상태별·정당별·직업군별 법안 수 시각화 (Plotly bar chart)

### 4. 지역구 Choropleth 지도
- 한국 GeoJSON(southkorea-maps 2018)으로 시도 단위 단색 choropleth 구현
- `st.plotly_chart(on_select="rerun")` 이벤트로 지도 클릭 → 시도 드릴다운
- GeoJSON 속성 키 자동 감지 (`CTP_KOR_NM` / `SIG_KOR_NM`)
- 지도 클릭과 버튼 클릭을 모두 지원하는 이중 인터랙션 구조

---

## 기술적 도전과 해결

### 처리상태 NULL로 인한 필터/차트 전체 무력화
- **상황**: API가 심사 중인 법안의 처리상태를 NULL로 반환 → `value_counts()`가 NaN 드롭 → 필터 옵션 빈 리스트, 차트 렌더링 실패
- **해결**: `load_bills()`에서 `df["status"].fillna("심사중")` 일괄 처리 + 모든 텍스트 컬럼 `fillna("")` 적용

### 정당 데이터 상시 공백
- **상황**: 열린국회정보 API의 `POLY_NM` 필드가 항상 빈 값으로 반환됨을 실 운영에서 발견
- **해결**: `members.csv`와 proposer 이름 조인. 정규식으로 "의원" suffix 제거 후 이름 추출 → 정당 매핑 (`re.sub(r"의원.*", "", proposer)`)

### Choropleth 지도 UX 재설계
- **초기 설계**: 시도 → 시군구 → 의원 3단계 드릴다운 choropleth
- **문제**: 시군구 GeoJSON 로딩 불안정 + 지도 크기 대비 정보 밀도 낮음
- **해결**: 지도(시도 choropleth) + 버튼 패널(시도/시군구) + 스크롤 가능한 의원 리스트 2-패널 구조로 재설계. `st.container(height=520)`으로 고정 높이 스크롤 영역 구현

### LLM 출력 품질 반복 개선
- **상황**: benefited/harmed_groups에 수식절 혼입, 정부기관이 사회집단으로 분류되는 오류
- **접근**: 잘못된 출력 패턴을 수집 → 근본 원인 분석 (모호한 지시어) → 프롬프트에 금지 패턴·예시 대조 추가 → 전체 재분석으로 결과 갱신
- **교훈**: LLM 출력 제어는 "하지 말 것"보다 "이렇게 해야 하는 예시"가 더 효과적

### 운영 자동화 중 Git lock 충돌
- **상황**: Google Drive 동기화가 `.git/index.lock`을 다른 기기에 복제 → 로컬 git push 블록
- **해결**: `find .git -name "*.lock" -delete`를 push 루틴에 포함, `.zshrc` alias 등록

---

## 데이터 규모 및 자동화 현황

| 항목 | 수치 |
|---|---|
| 수집 법안 수 | 100건 (증분 수집 중) |
| LLM 분석 완료 | 100건, 실패율 0% |
| 의원 데이터 | 300명 |
| 자동화 주기 | 수집 매시간 / 분석 매시간 30분 / 배포 매일 03:00 |
| LLM 추론 속도 | 약 60~90초/건 (로컬 Gemma 4, Apple Silicon) |

---

## 다룬 역량

- **데이터 엔지니어링**: REST API 수집, SQLite 스키마 설계·마이그레이션, 증분 upsert, 자동 배포 파이프라인
- **LLM 활용**: 로컬 모델(Ollama) 운영, 구조화 JSON 출력을 위한 프롬프트 엔지니어링, 출력 품질 반복 개선
- **데이터 분석 및 시각화**: Pandas 기반 필터링·집계, Plotly choropleth / bar chart
- **제품 사고**: 사용자 관점에서 기능 설계, 작동하는 MVP 우선 → 품질 반복 개선
- **운영 자동화**: cron 기반 파이프라인 전 단계 무인 운영

---

## 향후 개선 방향

- 첫 방문 온보딩(직업·주거형태 등 간단한 질문) → 자동 맞춤 필터 적용
- `benefited_groups` / `harmed_groups`를 "나에게 관련된 법안" 필터에 통합
- 관심 키워드·직업군 법안 발의 시 알림 기능

---

*데이터 출처: 열린국회정보 (open.assembly.go.kr) | 직업 분류: 통계청 한국표준직업분류(KSCO) 8차*
