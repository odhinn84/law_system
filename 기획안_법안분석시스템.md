# 국회 법안 분석 시스템 기획안

> 국회 발의/통과 법안을 자동 수집·분석하여 영향받는 사회집단을 분류하고 웹으로 공개하는 시스템

---

## 1. 시스템 개요

### 목적
- 일반 시민이 복잡한 법안의 **나와 관련성**을 즉시 파악할 수 있도록 지원
- 법안별 영향 집단(직업군, 연령대, 사회적 집단 등)을 LLM으로 자동 분류
- 주기적 데이터 갱신 + Streamlit 대시보드로 누구나 접근 가능한 형태로 공개

### 핵심 플로우

```
국회 API → 법안 수집 → Ollama(로컬 LLM) 분석/분류 → 결과 CSV 정리
→ GitHub push → Streamlit Cloud 자동 배포 → 웹 공개
```

---

## 2. 전체 아키텍처

```
로컬 머신 (항상 켜짐)
  └── 매일 1회 cron job
        ├── 1. 국회 Open API → 신규 법안 수집
        ├── 2. Ollama(gemma4:26b) → 요약 + 집단 분류 분석
        ├── 3. 분석 결과를 CSV로 정리 (원문 제외)
        └── 4. git commit & push → GitHub 레포

GitHub 레포
  ├── app/          (Streamlit 앱 코드)
  └── data/         (분석 결과 CSV — 매일 갱신)
        ├── bills.csv          # 법안 목록 + 분석 결과
        ├── last_updated.json  # 마지막 갱신 시각
        └── stats.json         # 통계 집계

Streamlit Cloud
  └── GitHub 레포 연동 → 데이터 변경 감지 → 자동 재배포 → 외부 공개
```

**원칙**: 원문 텍스트 등 무거운 데이터는 로컬에만 보관.
GitHub에는 Streamlit 표시에 필요한 정리된 데이터만 push.
원문이 필요한 사용자는 의안정보시스템 링크로 직접 이동.

---

## 3. 데이터 수집

### 3-1. 주요 데이터 소스

| 소스 | URL | 제공 데이터 | 인증 |
|------|-----|-------------|------|
| 열린국회정보 Open API | open.assembly.go.kr/portal/openapi | 의안 목록, 상세, 투표 결과 | API 키 (무료) |
| 공공데이터포털 | data.go.kr | 의원 발의법률안, 의원 정보 | API 키 (무료) |
| 의안정보시스템 (링크용) | likms.assembly.go.kr | 법안 원문 PDF | — |

### 3-2. 수집 대상 필드

```python
{
  "bill_id":      "의안번호",
  "title":        "법안명",
  "proposer":     "대표발의자",
  "party":        "정당",
  "propose_date": "발의일",
  "committee":    "소관위원회",
  "status":       "처리상태",   # 접수/심사/가결/부결/철회
  "summary":      "제안이유 및 주요내용",
  "link":         "의안정보시스템 원문 링크"
}
```

### 3-3. 수집 전략

- **초기 수집**: 22대 국회 전체 법안 (이후 21대 추가)
- **증분 수집**: 매일 cron — 마지막 수집일 이후 신규 법안만 처리
- **로컬 SQLite**: 수집 원본 + 분석 결과 전체 보관 (히스토리 관리용)

---

## 4. LLM 분석 파이프라인

### 4-1. 분석 단계

```
[제안이유 + 주요내용 텍스트]
    ↓
[1단계] 시민 친화적 요약 (200자 이내)
    ↓
[2단계] 영향 집단 분류 (JSON structured output)
    ↓
[3단계] 영향 방향 판단 (긍정/부정/혼재/중립)
    ↓
[4단계] 검색용 키워드 추출
```

### 4-2. LLM 설정 (Ollama)

```python
from openai import OpenAI

client = OpenAI(
    base_url="http://localhost:11434/v1",
    api_key="ollama",  # 더미값
)

response = client.chat.completions.create(
    model="gemma4:26b",   # ollama list로 확인한 실제 모델명
    messages=[{"role": "user", "content": prompt}],
    response_format={"type": "json_object"},
)
```

**안정성 처리**:
- JSON 파싱 실패 시 최대 3회 재시도
- 재시도 후에도 실패하면 해당 법안 건너뛰고 로그 기록

### 4-3. 영향 집단 분류 체계

#### 직업군
LLM이 법안 내용 기반으로 자유롭게 도출 (구체적 직업명 사용, 예: 의사, 건설업자 등)
추후 국가 직업 분류 체계(한국표준직업분류 등) 도입 가능

#### 연령대
`아동(0~12)`, `청소년(13~18)`, `청년(19~34)`, `중장년(35~59)`, `노년(60+)`

#### 사회적 집단
`취약계층`, `장애인`, `여성`, `다문화가정`, `1인가구`, `저소득층`,
`임신·육아`, `학부모`, `세입자`, `자가소유자`, `중소기업`, `대기업`

#### 지역
`서울`, `수도권`, `비수도권`, `농어촌`, `도심`, `지방중소도시`

### 4-4. GitHub에 올라가는 CSV 컬럼

```
bill_id, title, proposer, party, propose_date, committee, status,
summary, core_change, impact_direction, impact_detail,
affected_occupations, affected_ages, affected_social, affected_regions,
keywords, urgency, link, analyzed_at
```

영향 집단은 파이프(`|`) 구분 문자열로 저장. 예: `"의료/보건|교육"`

---

## 5. Streamlit 웹 앱 설계

### 5-1. 페이지 구성

```
├── 홈 (대시보드)
│   ├── 최근 발의/통과 법안 Top 10
│   ├── 이번 주 주목 법안
│   └── 집단별 법안 수 요약 차트
│
├── 법안 검색/탐색
│   ├── 키워드 검색
│   ├── 필터: 처리상태 / 위원회 / 기간
│   └── 법안 카드 목록 (영향 집단 태그 + 원문 링크)
│
├── 나에게 관련된 법안
│   ├── 직업 / 연령 / 지역 선택 → 맞춤 법안 목록
│   └── 영향 방향 (긍정/부정/혼재) 필터
│
└── 통계/분석
    ├── 집단별 법안 영향 히트맵
    ├── 정당별 발의 현황
    └── 처리상태 추이 (시계열)
```

### 5-2. 데이터 로딩 방식

Streamlit Cloud에서 GitHub raw URL로 CSV 직접 로드:

```python
import pandas as pd

DATA_URL = "https://raw.githubusercontent.com/{user}/{repo}/main/data/bills.csv"

@st.cache_data(ttl=3600)  # 1시간 캐시
def load_data():
    return pd.read_csv(DATA_URL)
```

---

## 6. 운영 자동화 (로컬 cron)

### 매일 실행 스크립트 흐름

```
pipeline.py
  ├── collect_new_bills()      # 국회 API → 로컬 SQLite
  ├── analyze_pending_bills()  # Ollama → 분석 결과 SQLite 저장
  ├── export_to_csv()          # SQLite → data/bills.csv 생성
  └── git_push()               # git add/commit/push → GitHub
```

### cron 설정 예시 (Mac launchd 또는 crontab)

```bash
# 매일 오전 6시 실행
0 6 * * * cd /path/to/project && python pipeline.py >> logs/pipeline.log 2>&1
```

### 로그 관리

```
logs/
├── pipeline_YYYYMMDD.log   # 실행 로그
└── errors.log              # 파싱/API 오류
```

---

## 7. 기술 스택

```
데이터 수집
├── requests / httpx       # 국회 API 호출
└── sqlite3                # 로컬 원본 저장

분석
├── openai (SDK)           # Ollama OpenAI 호환 API
└── pandas                 # CSV 정리/가공

웹 앱
├── Streamlit              # UI
├── plotly                 # 차트
└── streamlit-folium       # 지역구 지도 (Phase 2)

배포/운영
├── crontab / launchd      # 로컬 스케줄러
├── GitHub                 # 데이터 + 코드 저장소
└── Streamlit Cloud        # 웹 호스팅 (GitHub 연동, 무료)
```

---

## 8. 개발 로드맵

### Phase 1 - MVP (2~3주)
- [ ] 국회 Open API 키 발급
- [ ] 수집 스크립트 + 로컬 SQLite 적재 (22대 국회)
- [ ] Ollama LLM 분석 파이프라인
- [ ] CSV export + GitHub push 자동화
- [ ] 기본 Streamlit 앱 (검색 + 법안 카드 + 상세 링크)
- [ ] Streamlit Cloud 배포

### Phase 2 - 고도화 (2~3주)
- [ ] "나에게 관련된 법안" 맞춤 필터
- [ ] 통계 대시보드 (히트맵, 차트)
- [ ] 21대 국회 이력 데이터 추가
- [ ] 오류 알림 (분석 실패 시 로컬 알림)

### Phase 3 - 확장 (선택)
- [ ] 법안 알림 구독 (이메일)
- [ ] 유사 법안 추천 (embedding 검색)

---

## 9. 필요한 준비물

| 항목 | 내용 | 비고 |
|------|------|------|
| 국회 Open API 키 | open.assembly.go.kr 무료 신청 | 발급 1~2일 소요 |
| Ollama 모델명 | `ollama list`로 확인 | gemma4:26b 등 |
| GitHub 레포 | 신규 생성 | public 또는 private |
| Streamlit Cloud 계정 | GitHub 계정으로 가입 | 무료 |

---

## 10. 디렉터리 구조

```
law_system/                    ← GitHub 레포 루트
├── data/
│   ├── bills.csv              # 분석 결과 (Streamlit이 읽는 파일)
│   ├── last_updated.json
│   └── stats.json
├── pipeline/                  # 로컬에서만 실행
│   ├── collect.py             # 국회 API 수집
│   ├── analyze.py             # Ollama 분석
│   ├── export.py              # CSV export
│   └── run_pipeline.py        # 전체 파이프라인 실행 진입점
├── app/
│   ├── main.py                # Streamlit 앱
│   └── pages/
│       ├── home.py
│       ├── search.py
│       ├── my_bills.py
│       └── stats.py
├── logs/                      # .gitignore에 추가
├── local_db/                  # .gitignore에 추가 (SQLite)
├── .env.example
├── .gitignore
└── requirements.txt
```
