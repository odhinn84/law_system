# 국회 법안 분석 시스템 기획안

> 국회 발의/통과 법안을 자동 수집·분석하여 영향받는 사회집단을 분류하고 웹으로 공개하는 시스템

---

## 1. 시스템 개요

### 목적
- 일반 시민이 복잡한 법안의 **나와 관련성**을 즉시 파악할 수 있도록 지원
- 법안별 영향 집단(직업군·연령대·사회적 집단 등)을 LLM으로 자동 분류
- 주기적 데이터 갱신 + Streamlit 대시보드로 누구나 접근 가능한 형태로 공개

### 핵심 플로우

```
국회 API → 법안 수집 → Ollama(로컬 LLM) 분석/분류 → 결과 CSV 정리
→ GitHub push → Streamlit Cloud 자동 배포 → 웹 공개
```

---

## 2. 전체 아키텍처

```
로컬 머신 (cron 상시 실행)
  ├── 매시간 정각   : 국회 Open API → 신규 법안 수집 (증분)
  ├── 매시간 30분   : Ollama(gemma4:26b) → 미분석 법안 50건 분석
  └── 매일 새벽 3시 : 분석 결과 CSV 생성 → git push → GitHub

GitHub 레포 (odhinn84/law_system)
  ├── app/          (Streamlit 앱 코드)
  └── data/
        ├── bills.csv          # 법안 목록 + 분석 결과 (매일 갱신)
        ├── last_updated.json  # 마지막 갱신 시각
        └── stats.json         # 통계 집계

Streamlit Cloud
  └── GitHub 레포 연동 → 코드/데이터 변경 감지 → 자동 재배포 → 외부 공개
```

**원칙**: 원문 텍스트 등 무거운 데이터는 로컬 SQLite에만 보관.
GitHub에는 Streamlit 표시에 필요한 정리된 CSV만 push.

---

## 3. 데이터 수집

### 3-1. 사용 API

| API | 용도 |
|-----|------|
| `nzmimeepazxkubdpn` | 22대 국회 법률안 목록 (bill_id, 제목, 발의자, 정당, 위원회, 처리상태) |
| `BPMBILLSUMMARY` | 제안이유 및 주요내용 전문 텍스트 |
| `ALLBILL` | 의안정보시스템 원문 링크 URL |

### 3-2. 수집 전략

- **초기 전체 수집**: `python collect.py --all` (22대 국회 전체, 약 17,000건)
- **증분 수집**: 매시간 정각 cron — `python collect.py --pages 5` (최신 500건 범위 확인, 신규 없으면 즉시 종료)
- **제안이유**: `BPMBILLSUMMARY` API로 별도 수집 (bill_no 기준), 0.3초 간격으로 API 부하 조절

### 3-3. 로컬 SQLite 스키마

```sql
-- 법안 원본
bills (bill_id PK, bill_no, title, proposer, party, committee,
       propose_date, status, summary, link, collected_at)

-- LLM 분석 결과
bill_analysis (bill_id PK, bill_summary, core_change,
               impact_direction, impact_detail,
               affected_occ, affected_age, affected_social, affected_region,
               keywords, urgency, analyzed_at, parse_failed)
```

---

## 4. LLM 분석 파이프라인

### 4-1. 모델

- **Ollama** (로컬, `http://localhost:11434/v1`) — OpenAI 호환 API
- **모델**: `gemma4:26b`
- **온도**: 0.1 (일관성 우선)
- **JSON 파싱 실패 시**: 최대 3회 재시도, 이후 `parse_failed=1`로 기록

### 4-2. 분석 출력 항목

```json
{
  "bill_summary":          "시민 친화적 요약 (200자 이내)",
  "core_change":           "기존과 달라지는 핵심 내용 (1~2문장)",
  "impact_direction":      "positive | negative | mixed | neutral",
  "impact_detail":         "긍정/부정 이유 간략히 (1문장)",
  "affected_occupations":  ["KSCO 중분류 목록에서 선택"],
  "affected_ages":         ["연령대 목록에서 선택"],
  "affected_social":       ["사회집단 목록에서 선택"],
  "affected_regions":      ["지역 목록에서 선택"],
  "keywords":              ["키워드1", "키워드2", "키워드3"]
}
```

### 4-3. 영향 집단 분류 체계

#### 직업군 — 한국표준직업분류(KSCO) 8차 중분류 (약 49개)

| 대분류 | 중분류 예시 |
|--------|------------|
| 관리자 | 공공·기업 고위직, 행정·경영지원 관리직 … |
| 전문가 및 관련 종사자 | 정보통신 전문가 및 기술직, 보건 전문가 및 관련직, 교육 전문가 및 관련직 … |
| 사무 종사자 | 경영·회계 관련 사무직, 금융 사무직 … |
| 서비스 종사자 | 경찰·소방·보안 서비스직, 돌봄·보건 서비스직 … |
| 판매 종사자 | 영업직, 판매직 |
| 농림어업 숙련 종사자 | 농업 숙련직, 임업 숙련직, 어업 숙련직 |
| 기능원 및 관련 기능 종사자 | 건설·광업 관련 기능직, 전기·전자 관련 기능직 … |
| 장치·기계 조작 및 조립 종사자 | 운전·운송 관련직, 화학 관련 장치·기계 조작직 … |
| 단순 노무 종사자 | 건설·광업 단순 노무직, 청소·경비 단순 노무직 … |
| 군인 | 군인 |

자유 텍스트 대신 표준 분류를 사용하여 필터링 일관성 확보.

#### 연령대
`아동(0~12)`, `청소년(13~18)`, `청년(19~34)`, `중장년(35~59)`, `노년(60+)`

#### 사회적 집단
`취약계층`, `장애인`, `여성`, `다문화가정`, `1인가구`, `저소득층`,
`임신·육아`, `학부모`, `세입자`, `자가소유자`, `중소기업`, `대기업`

#### 지역
`서울`, `수도권`, `비수도권`, `농어촌`, `도심`, `지방중소도시`

---

## 5. Streamlit 웹 앱

### 5-1. 페이지 구성 (`st.navigation` 링크 방식)

```
🏠 홈
   - 전체 법안 수 / 분석 완료 수 / 심사중 법안 수 메트릭
   - 최근 분석된 법안 카드 목록

🔍 법안 검색
   - 키워드 검색
   - 필터: 처리상태 / 위원회 / 발의 기간

👤 나에게 관련된 법안
   - KSCO 대분류 선택 → 중분류 멀티셀렉트 (2단계 필터)
   - 연령대 / 사회집단 필터
   - 법안 카드에 긍정/부정 방향 표시 (개인화 맥락에서만 노출)

🗺️ 의원별 법안
   - 의원 이름 검색 → 해당 의원 발의 법안 목록

📊 통계
   - 직업군별 법안 영향 집계
   - 정당별 발의 현황
   - 처리상태 분포

ℹ️ 서비스 소개
   - 시스템 설명, 데이터 출처, LLM 분석 방식, 모델 정보, KSCO 기반 분류 설명, 주의사항
```

### 5-2. 데이터 로딩

```python
DATA_URL = "https://raw.githubusercontent.com/odhinn84/law_system/main/data/bills.csv"

@st.cache_data(ttl=3600)
def load_data():
    return pd.read_csv(DATA_URL)
```

---

## 6. 운영 자동화 (cron 3개)

```bash
# 수집 — 매시간 정각
0 * * * * cd pipeline && python collect.py --pages 5 >> logs/collect.log 2>&1

# 분석 — 매시간 30분 (50건/회)
30 * * * * cd pipeline && python analyze.py >> logs/analyze.log 2>&1

# 배포 — 매일 새벽 3시
0 3 * * * cd pipeline && python export.py >> logs/export.log 2>&1
```

**웹페이지 갱신 주기**: 매일 1회 (새벽 3시 이후)

---

## 7. 기술 스택

```
데이터 수집
├── requests           # 국회 Open API 호출
└── sqlite3            # 로컬 원본 + 분석 결과 저장

분석
├── openai (SDK)       # Ollama OpenAI 호환 API
├── ollama gemma4:26b  # 로컬 LLM
└── pandas             # CSV 정리/가공

웹 앱
├── Streamlit >= 1.36  # UI (st.navigation 사용)
└── plotly             # 차트

배포/운영
├── crontab            # 로컬 스케줄러 (수집/분석/배포 3개)
├── GitHub             # 데이터 + 코드 저장소
└── Streamlit Cloud    # 웹 호스팅 (GitHub 연동, 무료)
```

---

## 8. 개발 로드맵

### Phase 1 - MVP ✅ 완료
- [x] 국회 Open API 키 발급
- [x] 수집 스크립트 + 로컬 SQLite (22대 국회 전체 ~17,000건)
- [x] KSCO 8차 중분류 기반 Ollama LLM 분석 파이프라인
- [x] CSV export + GitHub push 자동화
- [x] Streamlit 앱 (검색 + 법안 카드 + 나에게 관련된 법안 + 의원별 + 통계 + 서비스 소개)
- [x] st.navigation 링크 방식 사이드바
- [x] Streamlit Cloud 배포
- [x] cron 3개 자동화 (수집/분석/배포)

### Phase 2 - 고도화 (예정)
- [ ] 21대 국회 이력 데이터 추가
- [ ] 분석 실패 법안 재분석 자동화
- [ ] export를 하루 여러 번으로 증가 (6시간마다 등)

### Phase 3 - 확장 (선택)
- [ ] 법안 알림 구독 (이메일)
- [ ] 유사 법안 추천 (embedding 검색)

---

## 9. 디렉터리 구조

```
law_system/                    ← GitHub 레포 루트
├── data/
│   ├── bills.csv              # 분석 결과 (Streamlit이 읽는 파일)
│   ├── last_updated.json
│   └── stats.json
├── pipeline/
│   ├── collect.py             # 국회 API 수집 (argparse: --all / --pages N)
│   ├── analyze.py             # Ollama 분석 (batch_size=50)
│   └── export.py              # CSV export + GitHub push
├── app/
│   └── main.py                # Streamlit 앱 (단일 파일)
├── local_db/                  # .gitignore — SQLite DB
├── logs/                      # .gitignore — cron 로그
├── .env                       # .gitignore — API 키 등
├── .env.example
├── .streamlit/                # .gitignore — 로컬 secrets.toml
├── .gitignore
├── crontab_setup.sh
└── requirements.txt
```

---

## 10. 환경 변수 (.env)

```
ASSEMBLY_API_KEY=...       # 열린국회정보 API 키
ASSEMBLY_UNIT=22           # 국회 대수
GITHUB_TOKEN=...           # CSV push용 Personal Access Token
GITHUB_REPO=odhinn84/law_system
OLLAMA_BASE_URL=http://localhost:11434/v1
OLLAMA_MODEL=gemma4:26b
```
